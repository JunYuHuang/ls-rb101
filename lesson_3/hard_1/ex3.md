# My Solution

`A)` prints:

```
one is: one
two is: two
three is: three
```

`B)` prints:

```
one is: one
two is: two
three is: three
```

`C)` prints:

```
one is: two
two is: three
three is: one
```
